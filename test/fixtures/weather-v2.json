[
  {
    "name": "get_forecast",
    "description": "Get the weather forecast for a location.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "location": { "type": "string" },
        "days": { "type": "string" },
        "units": { "type": "string" }
      },
      "required": ["location", "units"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "summary": { "type": "string" },
        "conditions": { "type": "string" }
      }
    }
  },
  {
    "name": "get_radar_map",
    "description": "Get a radar map image URL for a location.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "location": { "type": "string" }
      },
      "required": ["location"]
    }
  }
]
